{
  "name": "changpt-cms-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: GitHub OAuth provider for the ChanGPT Decap CMS",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
